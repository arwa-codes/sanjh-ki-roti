version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: sanjh_ki_roti_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgrespassword
      POSTGRES_DB: saanjh_ki_roti
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d saanjh_ki_roti"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
